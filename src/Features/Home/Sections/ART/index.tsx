import { FC } from "react";
import { Container } from "@/Components/Container";
import styles from "./styles.module.scss";

export const ART: FC = () => {
  return <Container className={styles.ART}>Art of Beauty</Container>;
};
